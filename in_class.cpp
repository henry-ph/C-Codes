#include<bits/stdc++.h>
using namespace std;

int main() {
    double x, y, z, f;
    x = -sqrt(10);
    y = -3 * x;
    z = 0;
    f = 12.5;
    cout << fixed << setprecision(2) << f << endl;
    return 0;
}